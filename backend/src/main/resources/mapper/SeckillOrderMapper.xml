<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shop.mapper.SeckillOrderMapper">

    <select id="countBySessionIdAndUserId" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM order_main
        WHERE seckill_session_id = #{sessionId}
          AND user_id = #{userId}
          AND order_type = 'seckill'
          AND status != 4
    </select>

    <select id="sumQuantityBySessionIdAndUserId" resultType="java.lang.Integer">
        SELECT COALESCE(SUM(oi.quantity), 0)
        FROM order_main om
        INNER JOIN order_item oi ON om.id = oi.order_id
        WHERE om.seckill_session_id = #{sessionId}
          AND om.user_id = #{userId}
          AND om.order_type = 'seckill'
          AND om.status != 4
    </select>
</mapper>
